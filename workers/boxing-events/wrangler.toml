# boxing-events — cards and official outcomes. NOT DEPLOYED. No cron:
# collection adapters for commissions/promoters/broadcasters are not approved.
# Secrets at deploy: SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY, BOXING_INTERNAL_TOKEN.

name = "boxing-events"
main = "src/index.mjs"
compatibility_date = "2026-09-01"
workers_dev = false

[observability]
enabled = true
head_sampling_rate = 1
