# Copy to .env (gitignored). Never commit real values.

# Disposable PostgreSQL server used by `npm run test:db`. Each test file
# creates and drops its own database. The harness refuses non-local hosts and
# anything that looks like a hosted Supabase project.
BOXING_TEST_DATABASE_URL=postgres://postgres@localhost:55432/postgres
